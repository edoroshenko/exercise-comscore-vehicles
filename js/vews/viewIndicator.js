var ViewIndicator = function(node) {
	this.node = node;
};

ViewIndicator.prototype.bindAll = function() {

};

ViewIndicator.prototype.reset = function() {
	// width of progressbar - a distance
	// fuel
	// oil
	// load (if specified)
};

ViewIndicator.prototype.update = function() {
	
};